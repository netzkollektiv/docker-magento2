#!/usr/bin/env bash

su www-data <<EOSU

    echo "getting Magento $MAGENTO_VERSION from git repository"
    cd /var/magento/magento2

    git fetch --all
    git checkout $MAGENTO_VERSION

    cp -r * $MAGEDIR
    cp .htaccess $MAGEDIR

EOSU
